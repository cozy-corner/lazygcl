pre-commit:
  commands:
    goimports:
      glob: "*.go"
      run: mise exec -- goimports -w {staged_files}
      stage_fixed: true
    tidy-check:
      glob:
        - "*.go"
        - "go.mod"
        - "go.sum"
      run: mise exec -- go mod tidy -diff
