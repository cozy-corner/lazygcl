pre-commit:
  parallel: true
  commands:
    gofmt:
      glob: "*.go"
      run: mise exec -- gofmt -w {staged_files}
      stage_fixed: true
    goimports:
      glob: "*.go"
      run: mise exec -- goimports -w {staged_files}
      stage_fixed: true
